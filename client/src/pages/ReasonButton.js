import { render } from "@testing-library/react";
import React, { useState, useEffect } from "react";
// import Button from "../components/Button"

function ReasonButton(props) {
    return (
        <div>
            <button className="block m-auto my-5 w-32 p-2 bg-lime1 text-white rounded-full font-bold focus:outline-none" onClick={() =>if ({ onClick() }}>Submit</button>
        </div >
    );
}
export default ReasonButton;