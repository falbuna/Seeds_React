const { response } = require("express")
const express = require("express");

const app = require("express");
const path = 


applicationCache.get("/isLoggedIn", function (req, res) {
    response.json({ loggedIn: true });
})